DATABASE_URL="mysql://familytree:familytree@localhost:3306/familytree"
# Signs the login session cookie — set your own random value in production
# (e.g. `openssl rand -hex 32`).
COOKIE_SECRET="change-me-to-a-random-secret"
